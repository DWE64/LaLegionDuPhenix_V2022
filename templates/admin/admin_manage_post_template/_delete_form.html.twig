<form method="post" action="{{ path('app_admin_manage_post_template_delete', {'id': post_template.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_template.id) }}">
    <button class="btn">{{ 'admin.manage.post.template.delete'|trans }}</button>
</form>
